<script>
  const sleep = ms => {
    return new Promise(resolve => {
      setTimeout(() => resolve(ms), ms)
    })
  }

  // sleep(3000).then((data) => {console.log(`After ${data} ms`)});
  // sleep(5000).then((data) => {console.log(`After ${data} ms`)});

  //export let data = []

  //console.log(data)

</script>
